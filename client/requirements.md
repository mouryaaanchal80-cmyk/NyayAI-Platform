## Packages
recharts | Data visualization for the Impact Dashboard
framer-motion | Page transitions and interactive animations
jspdf | Generating downloadable PDF complaint letters
lucide-react | Standard icon set for UI

## Notes
- `jspdf` is required for the Auto Complaint Generator download feature.
- `recharts` is required for the Impact Dashboard to show data visualization.
- `framer-motion` adds premium feeling animations suitable for a competition-winning project.
- Assuming the Replit Audio integration exists at `client/replit_integrations/audio/useVoiceRecorder.ts` for Voice Input functionality as specified.
- API endpoints inferred: POST `/api/complaints` for analysis, POST `/api/stt` for Speech-to-Text, GET `/api/stats` for dashboard data.
